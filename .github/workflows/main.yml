on:
  push:
    branches: [main]
    workflow_dispatch:
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@main

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.3
      with:
        server: ftp://72.105.23.254
        username: root
        password: ${{ secrets.FTP_PWD_VPS_KB }}
        local-dir: ./
        server-dir: /var/www/